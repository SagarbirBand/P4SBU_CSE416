/** @type {import('next').NextConfig} */

/*const withPlugins = require("next-compose-plugins");
const withNextOnNetlify = require("next-on-netlify");

const nextConfig = {
  experimental: {
    appDir: true, // Enables the new app directory (Next.js 13+)
  },
};*/

module.exports = {
  
}