// next.config.js (CommonJS)
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable the experimental app directory
  experimental: {
    appDir: true
  }
};

module.exports = nextConfig;
